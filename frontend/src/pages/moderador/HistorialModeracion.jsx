// Historial completo de moderación — redirige al componente principal
import { Navigate } from "react-router-dom";
export default function HistorialModeracion() {
  return <Navigate to="/moderador/ofertas" replace />;
}
